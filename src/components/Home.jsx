import React from 'react';

export const metadata = {
    title: "Home",
    description: "Home Page"
}

const HomePage = () => {
    return (
        <div>
            Home
        </div>
    );
};

export default HomePage;